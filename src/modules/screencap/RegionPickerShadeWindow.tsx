/** Passive per-display shade shown while the interactive picker follows the pointer. */
export default function RegionPickerShadeWindow() {
  return <div className="qx-region-picker-shade-window" aria-hidden="true" />;
}
