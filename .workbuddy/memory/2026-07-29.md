# 2026-07-29

## Release v0.6.48

Bumped version from 0.6.47 → 0.6.48 across package.json, package-lock.json,
Cargo.toml, Cargo.lock, tauri.conf.json, README.md.

Changes since v0.6.47 (17 files):
- Plugin workbench UI (DragSource, template args, toolbar refresh)
- Plugin marketplace + dev guide docs sync
- PluginManager rating feedback display fix
- QxReplyList fix in shell lists
- Module port inventory + check script updated
- CSS for shell/lists refined
- Marketplace mod fix

**Validation:**
- `npm run check` (architecture + docs + i18n + shell + island + module-ports) — all passed
- `cargo fmt --check` — passed
- `cargo check` — passed (existing warnings only)

**Pushed:** `main` → origin (GitHub) with tag `v0.6.48`.

## Previous: context panel overflow fix (2026-07-28)

Fixed right-side Context Panel entry overflow on Launcher home by adding
`min-width: 0` to `.qx-context-entry`, `.qx-context-entry-title`,
`.qx-context-entry-subtitle` in `src/styles/launcher.css`. Flex items now
shrink below content intrinsic min-width, so `text-overflow: ellipsis`
properly clips long plugin descriptions inside the `--qx-context-w` rail.
