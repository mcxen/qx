# Qx 当前任务与验证状态

> 状态：Current · 基线：v0.6.103 · 最后整理：2026-09-06
>
> 本文件只保留正在进行、明确阻塞或下一次必须复核的工作。已完成记录、旧实现计划和未重新确认的历史验收项保存在 [`docs/archive/task-ledger-through-v0.6.101.md`](docs/archive/task-ledger-through-v0.6.101.md)，不会自动作为当前待办继续累积。

## 维护约定

- 每个条目必须写清状态、范围和可验证的完成条件；不把架构正文复制到这里。
- 完成后只保留本次发布周期需要追溯的简短证据；发版后移入历史台账。
- 自动检查、浏览器回归、签名桌面复核、Windows Action、发布和线上资产是不同证据，不互相替代。
- 当前接口与 UI 规则分别以 [`docs/README.md`](docs/README.md) 和 [`UI_SPEC.md`](UI_SPEC.md) 为准。

## 当前工作

### BluePrint Masonry 落地与安装态纠偏（2026-09-06）

- 用户明确要求真实瀑布流，旧等高虚拟行不算完成。Luna Max 负责宿主几何和 BluePrint 消费者，主代理负责接口复核、组合消融与交付。
- 已定位旧截图原因：本机 Qx 已是 0.6.103，但 `~/.qx/plugins/blueprint` 仍只有 endpoint/PAT 的旧 manifest 与旧运行入口；Logo 与当前源码一致，不能只替换宿主二进制。
- 宿主 Cards 改为按源顺序逐项放入最短列、自然高度、1–3 列；几何邻居驱动方向键，保留视口虚拟化及选中/编辑项。修复了 border-box 漏算、末项测量后滚入视口和换宽测量失效的问题。
- 消融：旧 List/Gallery、Cards 数据边界、编辑回执、设置保存队列通过；浏览器合成 80 条笔记在 320/640/980 宽度分别仅挂载 6/11/17 项，无横溢出；20 行草稿撑高后重新补位。连续 640→980→320→980 并切换主题均无重叠；末项 79 跳转可见（16 项挂载）。编辑中滚动、换列保留草稿组件，不回到顶部。
- BluePrint 默认 Cards、空标题和显式 List 偏好回归通过。Host 编辑回执 30 秒覆盖 MCP 25 秒预算；插件成功保存后有界回放同会话同正文重试，避免重复更新。
- 0.6.104 最终 ARM64 包已重建，Apple Development 签名与 strict 校验通过；`npm run check`、tsc/build、cargo fmt/check 均通过。发现用户正在操作 Qx，替换暂缓等待可重启确认；旧应用和 BluePrint 包备份位于 `~/.qx/local-backups/`，未改 PAT/设置/笔记。真实上游写路径等待明确测试条目授权，未发布 BluePrint 新市场包；合成 fixture 不属于真实接口证据。
- BluePrint 源码提交 `acec77d` 已推送 `codex/blueprint-masonry-reviewed` 开发分支；避免推入会自动打包上架的 main，待真实接口门禁通过后合并发布。宿主公共端口不含服务凭据/业务分支，可独立按 0.6.104 发布。

### Fix — 下拉框按内容适宽

**状态**：共用 Select、设置尾部轨道、Shell 筛选和插件来源行已优化，用户确认效果并要求提交全部 diff。本机包已构建、签名、安装并重启。

- 短选项不再固定铺满；长选项触发器省略、菜单有界换行，箭头不被挤压。
- 插件来源不再以 260px basis 拉伸并挤走升级按钮；托盘/GIF 参数一起移除多余固定宽度。
- 连续 Workbench 表单和录屏参数栅格保留明确的整列布局。
- 首轮 `npm run check`、TS/Vite 构建通过；原生控件扫描仅命中既有 Markdown checkbox 样式。
- Luna Max 已准备四模式生产组件消融夹具；完整浏览器矩阵尚未收集。原生自动化进入设置前遇到窗口隐藏，未将其记为自动视觉验收通过；用户随后确认“没问题了”。
- 提交前全量门禁、BluePrint mock smoke 和日历 smoke 再次通过；本机应用签名验证通过。
- 已有未完成并行工作使聚合改动超过 1200 行；其真实接口发布门禁仍未满足，不将并行内容作为本轮已验收发布。

### Feature — 原生内容卡片与 BluePrint 样板

**状态**：实现与本地构建已落地；安装态组合验收、真实 MCP 验收和发布仍未完成。

- 宿主负责内容优先卡片、双击编辑会话、焦点/IME/Esc 与保存回执；插件负责鉴权、完整正文、版本冲突和写入。
- 设置采用声明式分组；连接字段显式保存，未声明新能力的插件保留原行为。
- BluePrint 为首个消费者；usememos 是后续复用目标，本轮不承诺其服务接口或发布。

**消融与完成条件**：

- [ ] 基线：不声明卡片/编辑/设置分组，List、Gallery、原 onInput 与偏好自动保存不变。
- [ ] 只开卡片：保序、空图不占位、长文/标签/窄窗口可读，单击选择、Enter 阅读。
- [ ] 再开编辑：双击正文进入；图片/链接不误触发；中文 IME、原生快捷键、保存与取消正确。
- [ ] 写入边界：只读无写入；失败/冲突保留草稿；超长原文不截断后覆盖；旧会话回包不覆盖新会话。
- [ ] 只开设置分组：旧插件不受影响，连接字段批量保存，密码不进入日志，失败保留输入。
- [ ] 组合：BluePrint 卡片/编辑/设置与真实 MCP 接口、面板销毁及重新打开验证。
- [ ] 同步作者文档、宿主架构和仓库插件开发技能；全量门禁、构建及签名桌面视觉验收。

**已取得的局部证据（不等同组合验收）**：

- 生产 Cards 组件浏览器夹具：320/640/980px 分别 1/2/3 列，无横向溢出；80 条记录仅挂载 5/10/12 张卡片。明暗主题和紧凑密度已目视检查。
- 单击选择、双击请求编辑、链接双击隔离通过；这里未挂载完整编辑会话，不能代替保存与 Esc 验收。
- Workbench bridge 行为测试覆盖来源/运行态伪造、后台 worker 隔离、迟到回包、销毁、身份字段与 UTF-8 上限。
- Rust preferenceGroups 的 6 项测试、格式和编译通过；设置提交值与未保存草稿隔离、在途 A/排队 B/自动保存 C 及失败回执测试通过。
- `npm run check`、前端构建和 `aarch64-apple-darwin --bundles app` 本地编译通过。开发包沿用 0.6.103 基线，不能冒充新的 tagged Release；未覆盖正在使用的 `/Applications/Qx.app`。
- BluePrint 自包含运行入口构建、mock smoke（含高级字段折叠）及临时副本 ZIP 完整性通过；官方 logo 保留。原位编辑、MCP/auth、表单投影按领域拆分，作者文档和仓库开发技能同步维护。
- 真实 MCP 验收仍缺测试 PAT 配置与测试写入范围确认；模拟服务结果不作为插件升级发布依据。
- 聚合改动已超过 1200 行发布阈值，但真实接口发布门禁尚未满足。并行日历插件改动保留、未纳入本次 BluePrint 验收或擅自发布；本轮未提交、打 tag 或推送。

### Feature — 社区插件 BluePrint 随手记

**状态**：实现、门禁、MCP 契约烟测、打包与本机插件目录安装完成；真实 PAT 桌面运行态待 Qx 0.6.103。

- Qx Workbench 表单补充宿主渲染的 64 KiB 长文本控件，供社区插件编辑正文。
- BluePrint 插件通过 Bearer PAT + 无状态 Streamable HTTP MCP 读取、创建和完整模型更新随手记，
  保留 `operationId`、`baseVersion`、附件 ID 与冲突重载边界。

**完成条件**：

- [x] Qx `npm run check` / `npm run build`
- [x] 插件 MCP mock smoke、打包与本地安装
- [ ] Qx 0.6.103 安装态使用真实 `content:read` + `content:write` PAT 完成查看、新建、编辑、冲突重载与鉴权图片预览

### Fix — Clipboard 100MB 图片显示卡死

**状态**：修复、全量门禁、构建和签名安装态大图复核完成。

- Clipboard 列表/详情不再走整图 `Vec<u8> → number[] → Uint8Array → Blob` IPC；前端只接收
  Qx 缓存路径并使用 Tauri asset URL。
- Rust 按源路径、大小、mtime、目标边长生成 320px 列表缩略图与 1600px 详情预览；单源限制
  128 MiB / 3200 万像素，派生缓存限制 256 MiB / 512 文件并沿用 Storage 清理注册表。
- 重解码串行进入 blocking 边界，Clipboard 自动 OCR 复用同一派生图；首屏不再重复执行
  `read_clipboard_image_now`，实时图片继续由原生监听器捕获。旧 raw byte IPC 只保留 8 MiB 兼容上限。

**完成条件**：

- [x] `npx tsc --noEmit` / `cargo check` / `cargo test --lib preview_tests`（4 tests）
- [x] 95,971,062 bytes BMP 定向回归、`npm run check` / `npm run build` / `cargo fmt --check`
- [x] 签名安装版 `/Applications/Qx.app`：91.5MB BMP 约 1 秒内显示；生成时 UI 状态读取
  116ms，连续 10 次上下切换 939ms，PageDown / Home 正常，进程 RSS 从约 273MB 峰值回落到约 105MB。

### Fix — Workbench 文章属性统一进入副标题

**状态**：宿主呈现、规范、自动门禁与本地可视回归已完成；只剩签名桌面 QxWeibo 复核。

- 资讯/社区 Detail 将顶层 `fields[]` 投影到标题下方元数据区，并去除副标题已有的作者、时间同值项。
- 管理型 Workbench 顶层字段与 `sections[].fields[]` 仍保留字段表语义。

**剩余验收**：

- [ ] 签名桌面 QxWeibo：宽/窄详情栏元数据自然换行，正文下方没有重复属性表；管理型 Workbench 字段表不变。

## 已确认、尚未排期

### Hacker News 帖子中英翻译

**状态**：只确认产品方向，尚未冻结接口或进入开发。

- 先用真实服务核对鉴权、额度、隐私、错误体和失败回退，再选择用户自有 AI API 或公开翻译 provider。
- 翻译 provider 必须是宿主窄端口，Hacker News 插件不得绑定单一供应商。
- 交互需明确正文/评论、单条/批量、取消和持久缓存边界；未完成真实接口验证前不发布。

## 本周期已完成

### Fix — 社区插件中文、拼音与缩写搜索

**状态**：搜索实现和 21 个插件声明词已完成；全量门禁、542 次双语目录搜索及消融通过，Apple Development 签名本机安装验收完成。未提交/发布。

- 普通/延迟注册共享搜索元数据；主搜索从已注册面板/命令生成结果，携带别名匹配分数。
- Manifest 自有中文、拼音、缩写词表；同数量元数据变化可刷新活动查询，保留输入防抖。
- 本机 21 个插件仅同步搜索关键词，保留各自运行代码、版本及偏好数据。
- 误匹配回归：排除说明字段，修复 `qui` 命中 V2EX 的 `requires token`；命令显示保留动作标题。反例与独立消融通过，全量门禁及签名安装完成，用户安装态截图确认 `qui` 外置插件仅剩快捷日历。
- `/Applications/Qx.app` 0.6.103 实测 `日历/rili/rl` 均显示外置快捷日历，并从中文搜索结果打开农历/调休表；`tq` 天气、`hkxw` Hacker News、`jinjia` 金价追踪均命中。macOS/Windows 共用 TS 搜索端口，Windows 安装态未执行。
- 完整 `tauri build -- --bundles app`（含 TS/Vite）和安装副本签名检查通过；构建包含工作区既有并行代码，未把这些代码归为本轮修复。
- 消融与覆盖范围见 [`docs/plugin-search-validation.md`](docs/plugin-search-validation.md)。

### Feature — 社区快捷日历农历与调休

**状态**：`raycast-calendar` 1.3.0 已打包、本机安装并经「已安装 → 重新扫描」加载；未发布到线上商店。

- 七列月历保留原偏好，增加离线农历/闰月、官方休/班标记和全年调休表；当前收录 2026 中国大陆官方安排，缺失年份明确提示。
- 旧 Raycast 转换运行时替换为 Qx Actions/locale/clipboard/openUrl 消费者；不修改内置模块或宿主业务代码。
- `smoke:calendar`（含上海/洛杉矶时区）、Qx `npm run check`、归档完整性通过；浏览器中英文 × 明暗 × 320/760px 无横向溢出。
- 本机 Qx 0.6.101 实际面板确认 v1.3.0、农历、9 月 20 日补班、中秋休假及全年调休表。Windows 安装态、真实 Actions 键盘逐项验收未执行。
- 本轮只构建插件自包含入口，未重建未改动的宿主 TS/Rust；保留并行开发内容，未提交/发布。

### Docs — 当前规范、研究提案与历史记录分层

**状态**：完成；变更量已触发 v0.6.102 发布门禁。

- 逐份核对仓库 Markdown 与当前代码、IPC、存储、Island、Shell 和插件维护策略。
- 将过时的任务台账、拟议架构、旧用户指南和停用 Agent 指令移入 `docs/archive/`。
- 将未落地方案移入 `docs/research/`，并为 Island、插件存储、用户指南和文档索引建立短而权威的当前版本。
- 修正文档中的失真事实，并让链接检查递归覆盖所有维护中的文档目录。

**证据**：

- [x] `npm run docs:check`
- [x] `npm run check` / `cargo fmt --check` / `cargo check`
- [x] `npm run build`
- [x] 最终变更量与 release cadence 门禁复核

### Fix — Home Dashboard 小窗口组件边界

**状态**：布局修复、自动验证、本地浏览器与签名桌面运行态复核完成。

- `640px` 容器断点覆盖 `.has-pinned` 宽屏规则，窄容器按“置顶入口 → 指标”切为单列。
- 宽屏紧凑等分时，置顶网格与指标列只在各自边界内滚动；RSS / Agent Usage 等整行卡片让 Dashboard 按真实内容增高。
- 相邻卡片无横向侵入，置顶应用顺序保持不变。

**证据**：

- [x] `npx tsc --noEmit` / `npm run check` / `npm run build`
- [x] 本地浏览器：980×576、620×500 与 RSS 整行卡片组合均无横向溢出。
- [x] v0.6.101 签名安装至 `/Applications/Qx.app` 后，真实原生 WebView 小窗口边界复核通过（2026-08-30）；旧日志未追加本次启动行，因此未把旧日志作为验收依据。

## 暂不继承的历史项

旧台账中的大量未勾选项来自已经结束的版本、当时无法运行的机器环境或后来被新协议替代的方案。需要继续时，应先根据当前代码重新确认问题仍存在，再以新的当前条目加入本文件；不要直接修改历史归档中的复选框。
