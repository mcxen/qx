# Qx 当前任务与验证状态

> 状态：Current · 基线：v0.6.102 · 最后整理：2026-08-31
>
> 本文件只保留正在进行、明确阻塞或下一次必须复核的工作。已完成记录、旧实现计划和未重新确认的历史验收项保存在 [`docs/archive/task-ledger-through-v0.6.101.md`](docs/archive/task-ledger-through-v0.6.101.md)，不会自动作为当前待办继续累积。

## 维护约定

- 每个条目必须写清状态、范围和可验证的完成条件；不把架构正文复制到这里。
- 完成后只保留本次发布周期需要追溯的简短证据；发版后移入历史台账。
- 自动检查、浏览器回归、签名桌面复核、Windows Action、发布和线上资产是不同证据，不互相替代。
- 当前接口与 UI 规则分别以 [`docs/README.md`](docs/README.md) 和 [`UI_SPEC.md`](UI_SPEC.md) 为准。

## 当前工作

### Fix — Workbench 文章属性统一进入副标题

**状态**：宿主呈现、规范、自动门禁与本地可视回归已完成；只剩签名桌面 QxWeibo 复核。

- 资讯/社区 Detail 将顶层 `fields[]` 投影到标题下方元数据区，并去除副标题已有的作者、时间同值项。
- 管理型 Workbench 顶层字段与 `sections[].fields[]` 仍保留字段表语义。

**剩余验收**：

- [ ] 签名桌面 QxWeibo：宽/窄详情栏元数据自然换行，正文下方没有重复属性表；管理型 Workbench 字段表不变。

## 已确认、尚未排期

### Hacker News 帖子中英翻译

**状态**：只确认产品方向，尚未冻结接口或进入开发。

- 先用真实服务核对鉴权、额度、隐私、错误体和失败回退，再选择用户自有 AI API 或公开翻译 provider。
- 翻译 provider 必须是宿主窄端口，Hacker News 插件不得绑定单一供应商。
- 交互需明确正文/评论、单条/批量、取消和持久缓存边界；未完成真实接口验证前不发布。

## 本周期已完成

### Docs — 当前规范、研究提案与历史记录分层

**状态**：完成；变更量已触发 v0.6.102 发布门禁。

- 逐份核对仓库 Markdown 与当前代码、IPC、存储、Island、Shell 和插件维护策略。
- 将过时的任务台账、拟议架构、旧用户指南和停用 Agent 指令移入 `docs/archive/`。
- 将未落地方案移入 `docs/research/`，并为 Island、插件存储、用户指南和文档索引建立短而权威的当前版本。
- 修正文档中的失真事实，并让链接检查递归覆盖所有维护中的文档目录。

**证据**：

- [x] `npm run docs:check`
- [x] `npm run check` / `cargo fmt --check` / `cargo check`
- [x] `npm run build`
- [x] 最终变更量与 release cadence 门禁复核

### Fix — Home Dashboard 小窗口组件边界

**状态**：布局修复、自动验证、本地浏览器与签名桌面运行态复核完成。

- `640px` 容器断点覆盖 `.has-pinned` 宽屏规则，窄容器按“置顶入口 → 指标”切为单列。
- 宽屏紧凑等分时，置顶网格与指标列只在各自边界内滚动；RSS / Agent Usage 等整行卡片让 Dashboard 按真实内容增高。
- 相邻卡片无横向侵入，置顶应用顺序保持不变。

**证据**：

- [x] `npx tsc --noEmit` / `npm run check` / `npm run build`
- [x] 本地浏览器：980×576、620×500 与 RSS 整行卡片组合均无横向溢出。
- [x] v0.6.101 签名安装至 `/Applications/Qx.app` 后，真实原生 WebView 小窗口边界复核通过（2026-08-30）；旧日志未追加本次启动行，因此未把旧日志作为验收依据。

## 暂不继承的历史项

旧台账中的大量未勾选项来自已经结束的版本、当时无法运行的机器环境或后来被新协议替代的方案。需要继续时，应先根据当前代码重新确认问题仍存在，再以新的当前条目加入本文件；不要直接修改历史归档中的复选框。
